{
  "name": "elevator-two",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "generative electro",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
